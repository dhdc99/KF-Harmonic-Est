clc
clear variables
close all

xk = 20*rand(20,1);
xk_est = xk + normrnd(0,1,20,1);
noise= xk - xk_est;
alpha=1;
kappa=0;

pk = ecm(xk,xk_est);

sig = sigmaselect(xk_est,pk,alpha,kappa);

newsig=sigmaupdate(s